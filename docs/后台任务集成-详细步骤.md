# 会议分析后台任务 - 快速集成代码

## 第1步：在 meetings.py 第498行之前添加辅助函数

在 `async def process_meeting_file(meeting_id: str):` 之前添加：

```python
async def update_task_progress(task_id: str, progress: int, db=None):
    """更新后台任务进度和资源占用"""
    if not task_id:
        return
    
    if db is None:
        db = get_database()
    
    try:
        import psutil
        process = psutil.Process()
        cpu_percent = process.cpu_percent(interval=0.1)
        memory_mb = process.memory_info().rss / 1024 / 1024
        
        await db.background_tasks.update_one(
            {"_id": ObjectId(task_id)},
            {
                "$set": {
                    "progress": progress,
                    "cpu_percent": cpu_percent,
                    "memory_mb": memory_mb,
                    "updated_at": get_beijing_time()
                }
            }
        )
    except Exception as e:
        logger.warning(f"更新任务进度失败: {e}")
```

## 第2步：修改 process_meeting_file 函数开始部分

找到第499-512行，将：

```python
async def process_meeting_file(meeting_id: str):
    """后台任务：处理会议文件"""
    import time
    start_time = time.time()
    logger.info(f"开始后台任务: 处理会议 {meeting_id}")
    db = get_database()
    
    try:
        # 获取会议记录
        meeting = await db.meetings.find_one({"_id": ObjectId(meeting_id)})
        if not meeting:
            logger.error(f"会议 {meeting_id} 不存在")
            return
```

替换为：

```python
async def process_meeting_file(meeting_id: str):
    """后台任务：处理会议文件"""
    import time
    import psutil
    start_time = time.time()
    logger.info(f"开始后台任务: 处理会议 {meeting_id}")
    db = get_database()
    task_id = None  # 后台任务ID
    
    try:
        # 获取会议记录
        meeting = await db.meetings.find_one({"_id": ObjectId(meeting_id)})
        if not meeting:
            logger.error(f"会议 {meeting_id} 不存在")
            return
        
        # 创建后台任务记录
        task_doc = {
            "task_type": "meeting_analysis",
            "task_name": f"会议分析: {meeting['title']}",
            "status": "running",
            "created_at": get_beijing_time(),
            "started_at": get_beijing_time(),
            "created_by": meeting["created_by"],
            "related_id": meeting_id,
            "progress": 0,
            "cpu_percent": 0.0,
            "memory_mb": 0.0
        }
        task_result = await db.background_tasks.insert_one(task_doc)
        task_id = str(task_result.inserted_id)
        logger.info(f"✅ 创建后台任务记录: {task_id}")
```

## 第3步：在音频提取后添加进度更新（约第533行）

在 `logger.info(f"音频提取成功 (耗时: {step_time:.2f}秒)")` 之后添加：

```python
            step_time = time.time() - step_start
            logger.info(f"音频提取成功 (耗时: {step_time:.2f}秒)")
            await update_task_progress(task_id, 10, db)  # 添加这行
```

## 第4步：在Whisper转录后添加进度更新（约第548行）

在 `logger.info(f"Whisper转录完成,共 {len(whisper_segments)} 个片段 (耗时: {step_time:.2f}秒)")` 之后添加：

```python
            step_time = time.time() - step_start
            logger.info(f"Whisper转录完成,共 {len(whisper_segments)} 个片段 (耗时: {step_time:.2f}秒)")
            await update_task_progress(task_id, 70, db)  # 添加这行
```

## 第5步：在转录完成后添加进度更新（约第620行）

在 `logger.info("转录完成，等待用户点击查看纪要按钮...")` 之后添加：

```python
        logger.info("转录完成，等待用户点击查看纪要按钮...")
        await update_task_progress(task_id, 90, db)  # 添加这行
```

## 第6步：在任务完成时更新状态（约第630行）

在保存转录结果的代码之后添加：

```python
        # 更新数据库 (保存转录结果，状态设为waiting_confirmation)
        update_data = {
            "status": MeetingStatus.WAITING_CONFIRMATION,
            "speakers": [s.dict() for s in speaker_segments],
            "transcript_file_path": transcript_file_path,
            "updated_at": get_beijing_time()
        }
        await db.meetings.update_one(
            {"_id": ObjectId(meeting_id)},
            {"$set": update_data}
        )
        
        # 更新后台任务为完成状态
        if task_id:
            await db.background_tasks.update_one(
                {"_id": ObjectId(task_id)},
                {
                    "$set": {
                        "status": "completed",
                        "progress": 100,
                        "completed_at": get_beijing_time()
                    }
                }
            )
            logger.info(f"✅ 后台任务完成: {task_id}")
```

## 第7步：在异常处理中更新失败状态（约第650行）

找到 except 块，在更新会议状态之前添加：

```python
    except Exception as e:
        logger.error(f"处理会议文件失败: {str(e)}")
        import traceback
        logger.error(f"详细错误: {traceback.format_exc()}")
        
        # 更新后台任务为失败状态
        if task_id:
            try:
                await db.background_tasks.update_one(
                    {"_id": ObjectId(task_id)},
                    {
                        "$set": {
                            "status": "failed",
                            "error_message": str(e),
                            "completed_at": get_beijing_time()
                        }
                    }
                )
                logger.info(f"❌ 后台任务失败: {task_id}")
            except Exception as update_error:
                logger.error(f"更新任务失败状态出错: {update_error}")
        
        # 更新会议状态为失败
        await db.meetings.update_one(
            {"_id": ObjectId(meeting_id)},
            {"$set": {
                "status": MeetingStatus.FAILED,
                "error_message": str(e),
                "updated_at": get_beijing_time()
            }}
        )
```

## 完成后

1. 保存 meetings.py 文件
2. 后端会自动重启（如果开启了reload）
3. 上传新的会议视频
4. 查看"后台任务"页面，应该能看到任务了

## 快速测试

```bash
# 重启后端
cd backend
python main.py
```

然后上传一个新的会议视频，立即查看后台任务页面。

---

**提示**: 如果不想手动修改，可以等我创建一个完整的新版本 meetings.py 文件。
