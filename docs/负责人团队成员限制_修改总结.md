# 负责人团队成员限制功能 - 修改总结

## 需求描述
确保在创建或编辑课题时，负责人（assigned_to）只能从该项目的团队成员中选择。

## 修改内容

### 1. 后端验证（`backend/routers/topics.py`）

#### 创建课题验证（已存在）
在 `create_topic` 函数中，第72-74行已经有验证逻辑：
```python
# 如果指定了分配对象，检查该用户是否在项目团队中
if topic.assigned_to:
    if topic.assigned_to not in project["team_members"]:
        raise HTTPException(status_code=400, detail="被分配用户不在项目团队中")
```

#### 更新课题验证（新增）
在 `update_topic` 函数中，第267-269行添加了验证逻辑：
```python
# 如果更新了负责人，检查新负责人是否在项目团队中
if "assigned_to" in update_data and update_data["assigned_to"]:
    if update_data["assigned_to"] not in project["team_members"]:
        raise HTTPException(status_code=400, detail="负责人必须是项目团队成员")
```

### 2. 前端限制（`frontend/src/views/Topics.vue`）

#### 2.1 添加计算属性 `availableUsers`
在第408-420行添加了计算属性，根据选择的项目过滤可用的负责人：
```javascript
// 根据选择的项目过滤可用的负责人
const availableUsers = computed(() => {
  if (!topicForm.project_id) {
    return []
  }
  const selectedProject = projects.value.find(p => p.id === topicForm.project_id)
  if (!selectedProject || !selectedProject.team_members) {
    return []
  }
  // 只返回该项目的团队成员
  return users.value.filter(user => selectedProject.team_members.includes(user.id))
})
```

#### 2.2 更新负责人选择器
在第145-164行修改了负责人选择器：
- 使用 `availableUsers` 替代 `users`
- 添加 `:disabled="!topicForm.project_id"` 禁用未选择项目时的选择
- 添加提示信息：
  - 未选择项目时：提示"请先选择所属项目"
  - 项目无团队成员时：提示"该项目暂无团队成员"

```vue
<el-form-item label="负责人">
  <el-select 
    v-model="topicForm.assigned_to" 
    placeholder="选择负责人" 
    clearable 
    style="width: 100%"
    :disabled="!topicForm.project_id"
  >
    <el-option
      v-for="user in availableUsers"
      :key="user.id"
      :label="`${user.full_name} (${user.username})`"
      :value="user.id"
    />
  </el-select>
  <div v-if="!topicForm.project_id" style="color: #999; font-size: 12px; margin-top: 5px;">
    请先选择所属项目
  </div>
  <div v-else-if="availableUsers.length === 0" style="color: #f56c6c; font-size: 12px; margin-top: 5px;">
    该项目暂无团队成员
  </div>
</el-form-item>
```

#### 2.3 添加项目变化监听器
在第838-850行添加了 `watch` 监听器：
```javascript
// 监听项目变化，如果当前负责人不在新项目的团队成员中，则清空
watch(() => topicForm.project_id, (newProjectId, oldProjectId) => {
  // 只在项目真正改变时处理
  if (newProjectId !== oldProjectId && topicForm.assigned_to) {
    const selectedProject = projects.value.find(p => p.id === newProjectId);
    if (selectedProject && selectedProject.team_members) {
      // 如果当前选择的负责人不在新项目的团队成员中，清空负责人
      if (!selectedProject.team_members.includes(topicForm.assigned_to)) {
        topicForm.assigned_to = '';
        ElMessage.warning('所选负责人不在该项目团队中，已自动清空');
      }
    }
  }
});
```

#### 2.4 导入 watch 函数
在第392行添加了 `watch` 的导入：
```javascript
import { ref, reactive, onMounted, computed, watch } from "vue";
```

## 功能特性

### 前端体验优化
1. **智能过滤**：负责人下拉列表只显示当前项目的团队成员
2. **禁用状态**：未选择项目时，负责人选择器被禁用
3. **友好提示**：
   - 未选择项目时显示灰色提示
   - 项目无团队成员时显示红色警告
4. **自动清空**：切换项目时，如果当前负责人不在新项目中，自动清空并提示

### 后端安全验证
1. **创建验证**：创建课题时验证负责人是否在项目团队中
2. **更新验证**：更新课题时验证新负责人是否在项目团队中
3. **错误提示**：验证失败时返回明确的错误信息

## 测试建议

1. **创建课题测试**：
   - 选择项目后，验证负责人列表只显示该项目成员
   - 尝试通过API直接提交非团队成员作为负责人，应返回400错误

2. **编辑课题测试**：
   - 修改负责人时，验证只能选择项目团队成员
   - 尝试通过API直接更新为非团队成员，应返回400错误

3. **项目切换测试**：
   - 选择项目A和负责人后，切换到项目B
   - 如果该负责人不在项目B中，应自动清空并显示提示

## 影响范围

- ✅ 后端：`backend/routers/topics.py`
- ✅ 前端：`frontend/src/views/Topics.vue`
- ✅ 用户体验：更加友好和安全
- ✅ 数据完整性：确保课题负责人始终是项目团队成员

## 部署说明

1. 后端会自动热重载（开发模式）
2. 前端会自动热重载（开发模式）
3. 无需数据库迁移
4. 无需额外配置

---

**修改完成时间**: 2026-01-27
**修改人**: AI Assistant
