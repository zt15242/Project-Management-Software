# 在 meetings.py 中集成后台任务管理

## 第1步：导入模块

在 `meetings.py` 文件顶部的导入部分添加：

```python
from background_task_manager import create_meeting_task
```

大约在第20-30行，其他导入语句之后添加。

## 第2步：修改 process_meeting_file 函数

### 原代码（约第499-512行）：

```python
async def process_meeting_file(meeting_id: str):
    """后台任务：处理会议文件"""
    import time
    start_time = time.time()
    logger.info(f"开始后台任务: 处理会议 {meeting_id}")
    db = get_database()
    
    try:
        # 获取会议记录
        meeting = await db.meetings.find_one({"_id": ObjectId(meeting_id)})
        if not meeting:
            logger.error(f"会议 {meeting_id} 不存在")
            return
        
        file_path = meeting["file_path"]
        file_type = meeting["file_type"]
        logger.info(f"会议文件: {file_path}, 类型: {file_type}")
```

### 修改为：

```python
async def process_meeting_file(meeting_id: str):
    """后台任务：处理会议文件"""
    import time
    start_time = time.time()
    logger.info(f"开始后台任务: 处理会议 {meeting_id}")
    db = get_database()
    task_manager = None  # 添加这行
    
    try:
        # 获取会议记录
        meeting = await db.meetings.find_one({"_id": ObjectId(meeting_id)})
        if not meeting:
            logger.error(f"会议 {meeting_id} 不存在")
            return
        
        # 创建后台任务记录
        task_manager = await create_meeting_task(
            meeting_id=meeting_id,
            meeting_title=meeting["title"],
            created_by=meeting["created_by"],
            db=db
        )
        
        file_path = meeting["file_path"]
        file_type = meeting["file_type"]
        logger.info(f"会议文件: {file_path}, 类型: {file_type}")
```

## 第3步：添加进度更新

### 3.1 音频提取完成后（约第533行）

在 `logger.info(f"音频提取成功 (耗时: {step_time:.2f}秒)")` 之后添加：

```python
            step_time = time.time() - step_start
            logger.info(f"音频提取成功 (耗时: {step_time:.2f}秒)")
            if task_manager:
                await task_manager.update_progress(10, "音频提取完成")
```

### 3.2 Whisper转录完成后（约第548行）

在 `logger.info(f"Whisper转录完成...")` 之后添加：

```python
            step_time = time.time() - step_start
            logger.info(f"Whisper转录完成,共 {len(whisper_segments)} 个片段 (耗时: {step_time:.2f}秒)")
            if task_manager:
                await task_manager.update_progress(70, "AI转录完成")
```

### 3.3 转录文本保存后（约第620行）

在 `logger.info("转录完成，等待用户点击查看纪要按钮...")` 之后添加：

```python
        logger.info("转录完成，等待用户点击查看纪要按钮...")
        if task_manager:
            await task_manager.update_progress(90, "转录完成")
```

## 第4步：任务完成时更新状态

在保存转录结果后（约第630行），添加：

```python
        # 更新数据库 (保存转录结果，状态设为waiting_confirmation)
        update_data = {
            "status": MeetingStatus.WAITING_CONFIRMATION,
            "speakers": [s.dict() for s in speaker_segments],
            "transcript_file_path": transcript_file_path,
            "updated_at": get_beijing_time()
        }
        await db.meetings.update_one(
            {"_id": ObjectId(meeting_id)},
            {"$set": update_data}
        )
        
        # 标记任务完成
        if task_manager:
            await task_manager.complete()
        
        total_time = time.time() - start_time
        logger.info(f"✅ 会议处理完成，总耗时: {total_time:.2f}秒")
```

## 第5步：异常处理中更新失败状态

在 except 块中（约第650行），修改为：

```python
    except Exception as e:
        logger.error(f"处理会议文件失败: {str(e)}")
        import traceback
        logger.error(f"详细错误: {traceback.format_exc()}")
        
        # 标记任务失败
        if task_manager:
            await task_manager.fail(str(e))
        
        # 更新会议状态为失败
        await db.meetings.update_one(
            {"_id": ObjectId(meeting_id)},
            {"$set": {
                "status": MeetingStatus.FAILED,
                "error_message": str(e),
                "updated_at": get_beijing_time()
            }}
        )
```

## 完整修改总结

需要修改的位置：

1. **第20-30行**: 添加导入 `from background_task_manager import create_meeting_task`
2. **第505行**: 添加 `task_manager = None`
3. **第512行后**: 创建任务管理器
4. **第533行后**: 更新进度 10%
5. **第548行后**: 更新进度 70%
6. **第620行后**: 更新进度 90%
7. **第630行后**: 标记任务完成
8. **第650行**: 标记任务失败

## 使用示例

修改完成后，当您上传新的会议视频时：

1. 系统会自动创建后台任务记录
2. 在"后台任务"页面可以看到：
   - 任务名称：会议分析: [会议标题]
   - 进度：0% → 10% → 70% → 90% → 100%
   - CPU和内存占用（实时更新）
   - 任务耗时
   - 任务状态

## 测试

1. 保存修改后的 meetings.py
2. 后端会自动重启
3. 上传一个新的会议视频
4. 立即打开"后台任务"页面
5. 应该能看到任务出现并实时更新进度

---

**优势**：
- ✅ 模块化设计，易于维护
- ✅ 代码简洁，只需几行调用
- ✅ 自动管理任务生命周期
- ✅ 支持暂停/继续功能（预留）
